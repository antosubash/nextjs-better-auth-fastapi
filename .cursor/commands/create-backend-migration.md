Create a new Alembic database migration for the backend.

This command will generate a new migration file based on model changes.

Usage: Provide a migration message/description when prompted.

Execute: `cd backend && uv run alembic revision --autogenerate -m "migration_description"`

Replace "migration_description" with a descriptive message about what the migration does.
