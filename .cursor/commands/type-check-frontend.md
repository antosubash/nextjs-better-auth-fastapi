Type check the frontend TypeScript code without building.

This command will run TypeScript compiler in check mode to verify type safety.

Execute: `cd nextjs && pnpm type-check`

This is useful for catching type errors before committing code.
