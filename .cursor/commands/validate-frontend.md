Validate frontend code quality (lint and format check without modifying files).

This command will:
1. Check frontend code with Biome (linting and formatting)
2. Validate constants usage

Execute: `cd nextjs && pnpm check`

This is useful for CI/CD or pre-commit checks.
