Seed the database with initial data.

This command will run the seed script to populate the database with initial admin user and other default data.

Execute: `cd nextjs && pnpm seed`

Note: Make sure migrations have been run before seeding.
